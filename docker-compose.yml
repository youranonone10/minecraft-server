version: '3.8'
services:
  minecraft:
    build: .
    ports:
      - "25565:25565"
    environment:
      RAM: 2048
    volumes:
      - minecraft-data:/minecraft/world
      - ./server.properties:/minecraft/server.properties
    restart: unless-stopped

volumes:
  minecraft-data:
